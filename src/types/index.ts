export interface Project {
  title: string;
  image: string;
  category: string;
}

export interface Categories {
  id: string;
  icon: string;
  title: string;
  previewImage: string;
}

export interface Process {
  icon: string;
  title: string;
  description: string;
}
